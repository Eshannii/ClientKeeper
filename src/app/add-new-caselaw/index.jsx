import { View, Text, Image, ScrollView } from "react-native";
import React from "react";
import AddCaselaw from "../../../components/AddCaselaw";

const index = () => {
  return (
    <ScrollView>
      <AddCaselaw />
    </ScrollView>
  );
};

export default index;
